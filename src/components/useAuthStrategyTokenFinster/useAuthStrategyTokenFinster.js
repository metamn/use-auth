import React from "react";
import PropTypes from "prop-types";

/**
 * Defines the prop types
 */
const propTypes = {};

/**
 * Defines the default props
 */
const defaultProps = {};

/**
 * Displays the component
 */
const useAuthStrategyTokenFinster = props => {
  return (
    <div className="useAuthStrategyTokenFinster">
      useAuthStrategyTokenFinster
    </div>
  );
};

useAuthStrategyTokenFinster.propTypes = propTypes;
useAuthStrategyTokenFinster.defaultProps = defaultProps;

export default useAuthStrategyTokenFinster;
export {
  propTypes as useAuthStrategyTokenFinsterPropTypes,
  defaultProps as useAuthStrategyTokenFinsterDefaultProps
};
