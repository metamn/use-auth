import React from "react";
import PropTypes from "prop-types";

/**
 * Defines the prop types
 */
const propTypes = {};

/**
 * Defines the default props
 */
const defaultProps = {};

/**
 * Displays the component
 */
const useAuthStrategyTokenFinsterAPI = props => {
  return (
    <div className="useAuthStrategyTokenFinsterAPI">
      useAuthStrategyTokenFinsterAPI
    </div>
  );
};

useAuthStrategyTokenFinsterAPI.propTypes = propTypes;
useAuthStrategyTokenFinsterAPI.defaultProps = defaultProps;

export { useAuthStrategyTokenFinsterAPI };
