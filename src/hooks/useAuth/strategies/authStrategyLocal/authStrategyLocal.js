import React from "react";
import PropTypes from "prop-types";

/**
 * Defines the prop types
 */
const propTypes = {};

/**
 * Defines the default props
 */
const defaultProps = {};

/**
 * Displays the component
 */
const authStrategyLocal = props => {
  return <div className="authStrategyLocal">authStrategyLocal</div>;
};

authStrategyLocal.propTypes = propTypes;
authStrategyLocal.defaultProps = defaultProps;

export default authStrategyLocal;
export {
  propTypes as authStrategyLocalPropTypes,
  defaultProps as authStrategyLocalDefaultProps
};
